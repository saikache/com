<%= form_for(@complaint) do |f| %>
  <% if @complaint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@complaint.errors.count, "error") %> prohibited this complaint from being saved:</h2>

      <ul>
      <% @complaint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company %><br>
    <%= f.select(:company_id, Company.all.collect {|u| [u.name, u.id]}, :prompt => 'Select') %>
  </div>



  <div class="field">
    <%= f.label :complaint_type_id %><br>
    <%= f.select(:complaint_type_id, ComplaintType.all.collect {|u| [u.name, u.id]}, :prompt => 'Select') %>
  </div>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
